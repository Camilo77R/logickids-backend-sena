export const REALTIME_ACTOR_TYPES = Object.freeze({
  web: 'web',
  student: 'student',
});

export const REALTIME_DOMAIN_EVENTS = Object.freeze({
  classSessionChanged: 'class_session:changed',
  rankingUpdated: 'ranking:updated',
  studentAccessChanged: 'student_access:changed',
});
